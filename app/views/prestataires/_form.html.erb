<div class="w3-container">

  <%= form_with(model: prestataire, local: true) do |form| %>
    <% if prestataire.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(prestataire.errors.count, "error") %> prohibited this prestataire from being saved:</h2>

        <ul>
        <% prestataire.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="w3-container">
      <%= form.label :categorie %>
      <%= form.text_field :categorie %>
    </div>

    <div class="w3-container">
      <%= form.label :nom %>
      <%= form.text_field :nom %>
    </div>

    <div class="w3-container">
      <%= form.label :remise %>
      <%= form.text_field :remise %>
    </div>

    <div class="w3-container">
      <%= form.label :prestation %>
      <%= form.text_field :prestation %>
    </div>

    <div class="w3-container">
      <%= form.label :description %>
      <%= form.text_area :description %>
    </div>

    <div class="w3-container">
      <%= form.label :ville %>
      <%= form.text_field :ville %>
    </div>

    <div class="w3-container">
      <%= form.label :imageurl %>
      <%= form.text_field :imageurl %>
    </div>

  <br>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
