<p id="notice"><%= notice %></p>

<!-- <h1>Begreen</h1> -->
<h2>Your restaurant eco friendly</h2>

<%= link_to 'Evaluer votre restaurant', new_restaurant_path %>

<br><br><br><br><br>
<% if user_signed_in? %>
  
<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>


<%else%>
<p>Please sign in or register to acces</p>
<%end%>
